{
  "name": "unreal-cyber-academy",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build:railway",
    "build:railway": "npm ci --prefix client --no-audit --no-fund && npm run build --prefix client && npm ci --prefix server --omit=dev --no-audit --no-fund",
    "start": "npm --prefix server start",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run start --prefix server\""
  },
  "dependencies": {
    "axios": "^1.13.2",
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
